using GlobalCoordinate.Runtime.Data;
using UnityEngine;

namespace jp.co.gatari.GlobalCoordinate.Sample
{
    public class Mapping : MonoBehaviour
    {
        private void Start()
        {
            var helmert = new HelmertTransform2D();
        }
    }
}